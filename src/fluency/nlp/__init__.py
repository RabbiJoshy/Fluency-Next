"""Model access shared by every mode.

Tagging a Spanish word does not depend on whether the sentence came from a
subtitle or a song lyric, so the machinery for it lives here rather than being
implemented once per mode.
"""
