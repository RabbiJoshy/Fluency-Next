"""Portuguese language policy."""

# Declared here rather than in a central list, so that adding a language means
# adding a package instead of editing a registry every other language shares.
LANGUAGE_CODE = "pt"
